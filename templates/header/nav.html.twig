<div class="container">
	<input type="checkbox" name="check" id="check"/>
	<div class="logo-container">
		<h3 class="logo">Badaboum<span>THÉÂTRE</span>
		</h3>
	</div>
	<div class="nav-btn">
		<div class="nav-links">
			<ul>
				{% for menu in menus %}
					{% if menu.isVisible == 1 %}
						<li class="nav-link">
							<a href="
								{{ menu.link }}
								">{{ menu.name|capitalize }}</a>
							{% if menu.subMenuses is not empty %}
								<div class="dropdown">
									<ul>
										{% for subMenu in menu.subMenuses %}
											{% if subMenu.isVisible == 1 %}
												<li class="dropdown-link">
													<a href="
														{{ subMenu.link }}
														">{{ subMenu.name|capitalize }}</a>
												</li>
												<div class="arrow"></div>
											{% endif %}
										{% endfor %}
									</ul>
								{% endif %}
							</li>
						{% endif %}
					{% endfor %}
				</ul>
			</div>
		</div>
		<div class="hamburger-menu-container">
			<div class="hamburger-menu">
				<div></div>
			</div>
		</div>
	</div>
<<<<<<< HEAD
</div>
{% if is_granted('IS_AUTHENTICATED_FULLY') %}
	<a class="dashboard" href="{{path('admin')}}">Tableau de bord</a>
{% endif %}

{# NEWSLETTER #}
{# <button id="btnNewsletter" class="logoNewsletter">N</button>
<form action="" class="formNewsletter"></form> #}
=======
	{% if is_granted('IS_AUTHENTICATED_FULLY') %}
		<a class="dashboard" href="{{path('admin')}}">Tableau de bord</a>
	{% endif %}
>>>>>>> 8f21615ffe1fdc236f5c60934d3e8f4e23f4ea5a

	{# NEWSLETTER #}
	{# <button id="btnNewsletter" class="logoNewsletter">N</button>
	<form action="" class="formNewsletter"></form> #}
