{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
	<div class="footernav">
		<div class="maps">
		<h3>Comment nous trouver</h3>
			<iframe id="map" src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2904.0264924420003!2d5.368758438345336!3d43.
			29276688228637!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x12c9c0c7018b8211%3A0xaa0c3f6a4b9118c2!2zQmFkYWJvdW0gVG
			jDqcOidHJl!5e0!3m2!1sfr!2sfr!4v1696250820067!5m2!1sfr!2sfr" 
			width="350" height="200" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
		</div>
		<div class="contactFooter">
			<h3>Contact</h3>
			<br>
			<p>16 quai de Rive-Neuve</p>
			<p>0491544071</p>
			<p>contact@badaboum-theatre.com</p>
		</div>
		<a class="contact_form" href="">Contactez-nous</a>
			{# AJOUTER FORM DE CONTACT #}
		{# <div class="navFooter">
			<h3>Navigation</h3>
			<ul>
				{% for menu in menus %}
					<li class="nav-link">
						{% if menu.link is not empty %}
							<a href="{{ menu.link }}">{{ menu.name|capitalize }}</a>
						{% else %}
							<a  href="#">
								{{ menu.name|capitalize }}
							</a>
						{% endif %}
					</li>
				{% endfor %}
			</ul>
		</div> #}
		{# <a class="newsletter" href="">Newsletter</a> #}
	

</div>
{% endif %}
<footer class="flex footer">
	<div>
		<p class="copyright">©
			{{ 'now'|date('Y') }}
			-  Crée par Yacine Djahafi. Tous droits réservés.</p>
	</div>
	<a id="legal" href="">Mentions Légales.</a>
	<ul class="flex adminAction">
		{% if is_granted('IS_AUTHENTICATED_FULLY') %}
			<li class="nav-item">
				<a href="{{path('app_register')}}" class="nav-link text-dark" aria-current="page">
					Inscription
				</a>
			</li>
		{% endif %}
		{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
			<li class="nav-item">
				<a href="{{path('app_login')}}" class="nav-link text-dark" aria-current="page">
					Pour l'équipe
				</a>
			</li>
		{% else %}
			<li>
				<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
			</li>
				<a class="nav nav-link text-dark dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
					{{ app.user.username|capitalize }}
				</a>
				<ul class="dropdown-menu">
					
				</ul>
			</div>
		{% endif %}
	</footer>
